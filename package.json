{
  "name": "@chatgpt/ui-platform",
  "private": true,
  "version": "0.8.0",
  "packageManager": "pnpm@10.15.0",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "perf:budget": "node scripts/check-bundle.mjs"
  },
  "devDependencies": { "prettier": "3.6.2", "turbo": "2.5.6", "typescript": "6.0.3" }
}
