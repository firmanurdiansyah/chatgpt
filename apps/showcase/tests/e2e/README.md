# Browser smoke tests

These tests are intentionally small and deterministic. They fail the CI quality gate when the deployed production bundle emits browser console errors, uncaught page errors, theme controls stop working, or the mobile viewport overflows horizontally.
