# Verification checkpoint

This file exists to trigger the pull-request CI gate for the UI platform foundation. The gate must pass typecheck, lint, test, and production build before the deployment commit is considered verified.
